[program:uwsgi]
command=uwsgi
  --enable-threads
  --vacuum
  --chdir /opt/labmanager-service-labbook
  --uid www-data
  --socket /tmp/app.sock
  -w service:app
  --master
directory=/opt/labmanager-service-labbook
autostart=true
autorestart=true
priority=999
stopsignal=INT

[program:nginx]
command=/usr/sbin/nginx

autostart=true
autorestart=true

priority=999